QUERY OVERPASS — ASIA-PACIFICO (l'ultimo grande passo)
Modo migliore: i comandi automatici qui sotto. I Paesi grandi (ID, IN, CN, JP, AU)
vanno col tiling (fetch_tiles.sh); gli altri col fetch normale (fetch_osm.sh).
Le query grezze servono solo se vuoi farne una a mano su https://overpass-turbo.eu .
======================================================================

>>> CLUSTER 13 — Sud-Est Asiatico (FIT MASSIMO: Bali, Phuket, beach club ovunque)
    bash scripts/fetch_osm.sh "TH,VN,PH,MY,KH,SG" ~/Desktop/lido-export
    bash scripts/fetch_tiles.sh ID ~/Desktop/lido-export     # Indonesia = giganti, a tasselli

  # Thailandia (TH)
  [out:json][timeout:600];area["ISO3166-1"="TH"][admin_level=2]->.a;(nwr["leisure"="beach_resort"](area.a);nwr["leisure"="marina"](area.a););out center tags;
  # Vietnam (VN)
  [out:json][timeout:600];area["ISO3166-1"="VN"][admin_level=2]->.a;(nwr["leisure"="beach_resort"](area.a);nwr["leisure"="marina"](area.a););out center tags;
  # Filippine (PH)
  [out:json][timeout:600];area["ISO3166-1"="PH"][admin_level=2]->.a;(nwr["leisure"="beach_resort"](area.a);nwr["leisure"="marina"](area.a););out center tags;
  # Malaysia (MY)
  [out:json][timeout:600];area["ISO3166-1"="MY"][admin_level=2]->.a;(nwr["leisure"="beach_resort"](area.a);nwr["leisure"="marina"](area.a););out center tags;
  # Cambogia (KH)
  [out:json][timeout:600];area["ISO3166-1"="KH"][admin_level=2]->.a;(nwr["leisure"="beach_resort"](area.a);nwr["leisure"="marina"](area.a););out center tags;
  # Singapore (SG)
  [out:json][timeout:600];area["ISO3166-1"="SG"][admin_level=2]->.a;(nwr["leisure"="beach_resort"](area.a);nwr["leisure"="marina"](area.a););out center tags;
  # Indonesia (ID) — meglio a tasselli: bash scripts/fetch_tiles.sh ID …

----------------------------------------------------------------------
CLUSTER 14 — Asia Meridionale & Oceano Indiano (Goa, Sri Lanka, Maldive)
    bash scripts/fetch_osm.sh "LK,MV,BD" ~/Desktop/lido-export
    bash scripts/fetch_tiles.sh IN ~/Desktop/lido-export

  # Sri Lanka (LK)
  [out:json][timeout:600];area["ISO3166-1"="LK"][admin_level=2]->.a;(nwr["leisure"="beach_resort"](area.a);nwr["leisure"="marina"](area.a););out center tags;
  # Maldive (MV)
  [out:json][timeout:600];area["ISO3166-1"="MV"][admin_level=2]->.a;(nwr["leisure"="beach_resort"](area.a);nwr["leisure"="marina"](area.a););out center tags;
  # Bangladesh (BD)
  [out:json][timeout:600];area["ISO3166-1"="BD"][admin_level=2]->.a;(nwr["leisure"="beach_resort"](area.a);nwr["leisure"="marina"](area.a););out center tags;
  # India (IN) — a tasselli: bash scripts/fetch_tiles.sh IN …

----------------------------------------------------------------------
CLUSTER 15 — Asia Orientale (Giappone, Corea, Taiwan, Cina)
    bash scripts/fetch_osm.sh "KR,TW,HK" ~/Desktop/lido-export
    bash scripts/fetch_tiles.sh JP ~/Desktop/lido-export
    bash scripts/fetch_tiles.sh CN ~/Desktop/lido-export

  # Corea del Sud (KR)
  [out:json][timeout:600];area["ISO3166-1"="KR"][admin_level=2]->.a;(nwr["leisure"="beach_resort"](area.a);nwr["leisure"="marina"](area.a););out center tags;
  # Taiwan (TW)
  [out:json][timeout:600];area["ISO3166-1"="TW"][admin_level=2]->.a;(nwr["leisure"="beach_resort"](area.a);nwr["leisure"="marina"](area.a););out center tags;
  # Hong Kong (HK)
  [out:json][timeout:600];area["ISO3166-1"="HK"][admin_level=2]->.a;(nwr["leisure"="beach_resort"](area.a);nwr["leisure"="marina"](area.a););out center tags;
  # Giappone (JP) e Cina (CN) — a tasselli (vedi comandi sopra)

----------------------------------------------------------------------
CLUSTER 16 — Oceania (Australia, Nuova Zelanda, Figi)
    bash scripts/fetch_osm.sh "NZ,FJ" ~/Desktop/lido-export
    bash scripts/fetch_tiles.sh AU ~/Desktop/lido-export

  # Nuova Zelanda (NZ)
  [out:json][timeout:600];area["ISO3166-1"="NZ"][admin_level=2]->.a;(nwr["leisure"="beach_resort"](area.a);nwr["leisure"="marina"](area.a););out center tags;
  # Figi (FJ)
  [out:json][timeout:600];area["ISO3166-1"="FJ"][admin_level=2]->.a;(nwr["leisure"="beach_resort"](area.a);nwr["leisure"="marina"](area.a););out center tags;
  # Australia (AU) — a tasselli: bash scripts/fetch_tiles.sh AU …

======================================================================
POI: lascia i file in ~/Desktop/lido-export (è connessa, li leggo io) e genero
le migrazioni 0031/0032/0033/0034 coi Paesi, bandiere e i continenti Asia + Oceania.
